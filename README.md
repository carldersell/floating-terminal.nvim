# floating-terminal.nvim

A lightweight terminal toggle plugin for Neovim with floating and bottom terminals, custom sizes, and overridable keymaps.

---

## ✨ Features

- Floating terminal with rounded borders
- Bottom terminal with adjustable height
- Commands for resizing
- Default keymaps (overridable)
- Lazy-friendly `setup()` interface

---

## 📦 Installation (Lazy.nvim)

```lua
{
  "carldersell/floating-terminal.nvim",
  opts = {
    keymaps = {
      toggle_floating = "<leader>tf",
      toggle_bottom = "<leader>tb",
    },
  },
}
# floating-terminal.nvim

A lightweight terminal toggle plugin for Neovim with floating and bottom terminals, custom sizes, and overridable keymaps.

---

## ✨ Features

- Floating terminal with rounded borders
- Bottom terminal with adjustable height
- Commands for resizing
- Default keymaps (overridable)
- Lazy-friendly `setup()` interface

---

## 📦 Installation (Lazy.nvim)

```lua
{
  "carldersell/floating-terminal.nvim",
  opts = {
    keymaps = {
      toggle_floating = "<leader>tf",
      toggle_bottom = "<leader>tb",
    },
  },
}
